Culina v5.4 — fichiers à mettre à la racine du dépôt GitHub :
- index.html
- recettes.json
- favicon.ico
- manifest.webmanifest
- service-worker.js
- dossier img/ complet

Le rappel local fonctionne automatiquement tant que Culina est ouvert (y compris en arrière-plan si le navigateur ne suspend pas la page). Si le navigateur ferme/suspend totalement le site, un site GitHub Pages statique ne peut pas garantir de réveiller le téléphone sans service de push externe.
